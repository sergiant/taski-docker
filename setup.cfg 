[flake8]
# Не проверять код на соответствие стандартам W503 и F811
ignore = 
    W503,   # Разрыв линии перед бинарным оператором
    F811     # Повторное определение имени (часто возникает в тестах и может быть ложным срабатыванием)

# Не проверять код в перечисленных директориях:
exclude = 
    tests/,
    */migrations/,
    venv/,
    */venv/,
    env/,
    */env/,
    .git,
    __pycache__,
    backend/collected_static/,
    static/,
    media/

# Не проверять указанные файлы на соответствие определённым правилам:
per-file-ignores =
    */settings.py:E501   # Игнорировать длинные строки в settings.py

# Дополнительные настройки для лучшей читаемости
max-line-length = 79
