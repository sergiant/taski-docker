[flake8]
# Не проверять код на соответствие стандартам W503 и F811, ДОБАВЛЯЕМ E501
ignore = 
    W503,   # Разрыв линии перед бинарным оператором
    F811     # Повторное определение имени (часто возникает в тестах и может быть ложным срабатыванием)
    E501    # ДОБАВЛЯЕМ - игнорировать длинные строки ВЕЗДЕ

# Не проверять код в перечисленных директориях:
exclude = 
    tests/,
    */migrations/*,
    venv/,
    */venv/*,
    env/,
    */env/*,
    .git,
    __pycache__,
    backend/collected_static/,
    static/,
    media/,
    *.pyc

# Дополнительные настройки для лучшей читаемости
max-line-length = 79

# Игнорировать E501 в конкретных файлах/паттернах
# per-file-ignores = 
#    */settings.py: E501,
#    backend/backend/settings.py: E501,    # ЯВНО указываем путь
#    backend/api/migrations/*: E501        # И миграции на всякий случай

